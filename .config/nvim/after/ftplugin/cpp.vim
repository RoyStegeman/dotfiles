setlocal colorcolumn=100 " add visual vertical column to 
setlocal tabstop=2 "show tab as 4 spaces
setlocal shiftwidth=2 " make tab equivalen to 4 spaces
setlocal expandtab=true " only use space characters, never tabs

